import { checkCommand } from './check.js';
import { syncCommand } from './sync.js';
import { finesseCommand } from './finesse.js';
import { setupClaimCommand } from './setup-claim.js';

export const commands = [checkCommand, syncCommand, finesseCommand, setupClaimCommand];
export { checkCommand, syncCommand, finesseCommand, setupClaimCommand };
